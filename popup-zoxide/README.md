# Popup zoxide

Bind the action to `prefix+z` in Herdr's config:

```toml
[[keys.command]]
key = "prefix+z"
type = "plugin_action"
command = "sunznx.popup-zoxide.open"
description = "search zoxide directories"
```
