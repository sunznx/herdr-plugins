# Herdr Duplicate

`duplicate-tab-or-agent` creates a new tab in the current workspace using the triggering pane's directory. If the current tab contains Codex or Claude, it starts the matching agent in the new tab.
